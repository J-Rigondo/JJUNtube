extends layouts/main

block content
    .form_container
        form(action="")
            input(type="text", name='title', placeholder='Title')
            textarea(name="description", placeholder='Description ',cols="6", rows="2")
            input(type="submit", value='Update Video')
    a.form_container_link(href=routes.deleteVideo) Delete Video